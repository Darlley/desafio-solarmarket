import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ['m.media-amazon.com'], // substitua pelo domínio da imagem externa
  },
};

export default nextConfig;
